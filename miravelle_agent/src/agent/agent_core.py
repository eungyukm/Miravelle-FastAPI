class Agent:
    def __init__(self):
        # 초기화 작업 (예: 모델 로드, 설정 등)
        pass

    def process(self, prompt: str) -> str:
        # 에이전트의 실제 처리 로직 구현 (여기서는 단순 echo)
        return f"에이전트 응답: {prompt}"
